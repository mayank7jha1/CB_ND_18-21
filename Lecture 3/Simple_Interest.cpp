// Created by Mayank

#include <iostream>
using namespace std;

int main() {
	int Principle, Rate, Time;
	cin >> Principle >> Rate >> Time;

	int SI = (Principle * Rate * Time) / 100;
	int may = 200;
	cout << SI << endl << may;


	// if (condition) {

	// }
	return 0;
}