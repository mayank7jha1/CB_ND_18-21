// Created by Mayank

#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	string s = "Bat";
	string s2 = "MayatBman";

	int found = s2.find(s);
	cout << found << endl;
	return 0;
}