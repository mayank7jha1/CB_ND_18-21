// Created by Mayank

#include <iostream>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	// Mayank
	// char ch;
	// ch = cin.get();
	// while (ch != '$') {
	// 	cout << ch;
	// 	ch = cin.get();
	// }
	char ch;
	while (cin >> ch) {
		cout << ch;
	}

	return 0;
}